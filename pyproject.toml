[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "termview"
version = "0.1.0"
dependencies = [
    "playwright",
    "Pillow",
]

[project.scripts]
termview = "termview:main"
